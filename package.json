{
  "name": "flare-monorepo",
  "version": "0.1.0",
  "private": true,
  "packageManager": "npm@10.5.0",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "turbo": "^2.5.3"
  }
}
